# Windows Server 2025 Security Baseline Automation

# เครื่องมือช่วยตรวจสอบและปรับปรุงความปลอดภัย (Audit & Remediation) สำหรับ Windows Server 2025 ตามมาตรฐาน CIS Benchmark อัตโนมัติ

## 📂 โครงสร้างสำคัญ
- **audit/**: สคริปต์ตรวจสอบความปลอดภัยทั้งหมด
- **remedtation/**: สคริปต์สำหรับแก้ไขช่องโหว่ทั้งหมด
- **cis_automation_v3.py**: สคริปต์หลักที่ใช้สำหรับรันงาน

## ⚙️ ความสามารถเด่น
- รันสคริปต์ Audit เพื่อประเมินสถานะของแต่ละ Control ตาม CIS benchmark ไม่มีค่าใช้จ่าย
- รัน Remediation ทั้งชุด (หรือเลือกเฉพาะ Control) ผ่าน PowerShell ที่ส่งขึ้นไปยังโฮสต์เป้าหมาย
- เก็บผลลัพธ์ทั้งในรูปแบบล็อกไฟล์ และสรุปในรูปแบบ HTML ภายใน `reports/` ด้วย Jinja2 template
- รองรับโหมด Dry-run เพื่อจำลองการแก้ไขก่อนลงจริง และสามารถสั่งข้าม Step ที่ไม่ต้องการได้จาก `configs/control.py`

## 🧾 Prerequisites
- ติดตั้ง Python 3.11+ และ dependencies จาก `requirements.txt` (ใช้ `pip install -r requirements.txt`)
- วางไฟล์ `tools/LGPO.exe` ไว้ในโฟลเดอร์ `tools/` เพื่อให้สคริปต์สามารถอัปโหลดไปยังเครื่องปลายทางแล้วใช้จัดการ GPO
- สร้าง `configs/hosts.json` ตามโครงสร้างด้านล่างเพื่อระบุโฮสต์ที่จะตรวจสอบ
- ตรวจสอบสิทธิ์ผู้ใช้งาน (ควรเป็น Administrator) และเปิด PowerShell ด้วย Execution Policy แบบ `Bypass` หรือ `Unrestricted`

## 🧭 วิธีใช้งานแบบละเอียด
1. กำหนดโฮสต์ปลายทางใน `configs/hosts.json` เช่น:

```json
{
  "hosts": [
    {
      "host": "192.168.150.153",
      "user": "Administrator",
      "password": "P@ssw0rd!"
    }
  ]
}
```

2. ปรับค่าคอนฟิก (ถ้าจำเป็น):
   - `configs/control.py` กำหนดว่าจะรัน Step ไหนบ้าง และควบคุมการข้ามขั้น (audit/remediation)
   - ตัวแปร `DRY_RUN` ใน `cis_automation_v3.py` (หรือโน้ตบุ๊ก) ปรับเป็น `True` เพื่อทดสอบ

3. รันสคริปต์หลัก:

```powershell
python cis_automation_v3.py
```

   - สคริปต์จะลูปผ่าน Controls ที่กำหนดไว้ใน `configs/cis_config.py`
   - แต่ละ Control จะส่งไฟล์ PowerShell ไปยัง `C:/Windows/Temp/cis_temp_exec.ps1` แล้วรันพร้อมบันทึกผล
   - ผลลัพธ์จะเขียนลง `logs/<host>/<timestamp>.log` และสร้างรายงาน HTML ภายใน `reports/`

4. ตรวจสอบผลลัพธ์:
   - ดูไฟล์ล็อกเพื่อวิเคราะห์ข้อผิดพลาด เช่น Registry path ผิดหรือ LGPO ไม่มี
   - เปิดไฟล์ในโฟลเดอร์ `reports/summary` เพื่อดูสรุปแบบกราฟิก

5. หากต้องการเจาะจง Control เดี่ยว ๆ ให้อาศัยพารามิเตอร์ `target_sections` (เช่น `1-9`) จากเมนูโต้ตอบใน Notebook หรือ CLI

## 🧩 หมายเหตุการดูแล
- สคริปต์ Remediation จะสร้างเนื้อหาของ LGPO เอง (`Computer`, `SOFTWARE\Policies\...`) และนำเข้า LGPO.exe หากมีอยู่แล้ว
- หากพบข้อผิดพลาด `Unable to create policy key \SOFTWARE\...` ให้ตรวจสอบว่าเซิร์ฟเวอร์มีสิทธิ์และพาธถูกต้อง (แพทช์ล่าสุดใน repo ได้แก้ path ให้ไม่มี slash นำหน้า)
- โครงสร้างการรายงานและโฮสต์สามารถปรับได้โดยแก้ `configs/cis_config.py` และไฟล์เทมเพลต HTML ใน `templates/` หากต้องการแสดงฟิลด์เพิ่มเติม

## 🧪 การตรวจสอบเพิ่มเติม
- ปรับ `configs/control.py` เพื่อเปิด/ปิด Step ตามกฎหมายองค์กร
- ลองรัน Dry-run ก่อนลงจริง โดยตั้ง `DRY_RUN = True` แล้วดูว่าข้อความ “DRY RUN” ปรากฏในล็อกหรือไม่
- หาก LGPO ไม่สามารถใช้งาน ให้ตรวจสอบว่า `tools/LGPO.exe` สามารถรันด้วยสิทธิ์เดียวกับผู้ใช้งานที่กำลังเชื่อมต่อ

## 🚀 วิธีเริ่มใช้งานแบบรวดเร็ว

### 1. ตั้งค่าเครื่องเป้าหมาย
สร้างไฟล์ `configs/hosts.json` (หากยังไม่มี) เพื่อระบุเครื่องที่ต้องการตรวจสอบ:
```json
{
  "hosts": [
    {
      "host": "127.0.0.1",
      "user": "Administrator",
      "password": "Password123"
    }
  ]
}
```

### 2. เริ่มทำงาน
เปิด Terminal ในโฟลเดอร์โปรเจกต์แล้วรันคำสั่ง:
```powershell
python cis_automation_v3.py
```
- ผลลัพธ์การตรวจสอบจะแสดงผลเป็นสรุป HTML ผ่านไฟล์ในโฟลเดอร์ `templates/`

